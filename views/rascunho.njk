<!-- Quando abre o service ele direciona atraves do extends essa pagina para a informada(index)-->
{% extends "layout.njk" %}

{% block head %}
<title>Portfolio - Mayk Brito</title>

<link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
{% endblock %}

{% block content %}
        
        <div id="wrapper">
            <h1>Aulas</h1>
        </div>

        <section class="cards">

            {% for item in items %}
            <div class="card" id="{{item.id}}">
                <div class="card__image-container {{ 'featured-wrapper' if item.featured }} ">
                    <img src="https://img.youtube.com/vi/{{item.id}}/maxresdefault.jpg" alt="Imagem de {{item.title}}">
                    {% if item.featured %}
                        <div class="featured">Featured</div>
                    {% endif %}
                </div>
                <div class="card__content">
                    <p>{{item.title}}</p>
                </div>
                <div class="card__info">
                    <p>{{item.duration}}</p>
                    <p class="card__price">{{item.price}}</p>
                </div>
            </div>
            {% endfor %}

        </section>

        <script src="/scripts.js"></script>


{% endblock %}




{# {% block content %}
  <section class="section" id="producers">
    <header>
      <a class="logo2">Li<span>food</span>.</a>
      <br>
      <a class="sublogo">Encontre<span> Negocie</span>.</a>
      <br>
      <br>
      <h3 class="subtitle">Encontre por aqui produtores orgânicos</h3>
      <button class="button">Pesquisar</button>
        <input 
          type="text"
          name="filter"
          class="text" 
          placeholder="Digite algum produto desejado"/>
    </header>
    
  </section>
  



  {% endblock %} #}